extends ../layouts/default

block main
  h1= financing.title

block content
  h2 Ficha do Financiamento
  p 
    | Data:
    =Moment(financing.contractDate).utc().format('DD/MM/YYYY')
  p 
    | Valor: #{Globalize.format(financing.amount,'c')}
  p 
    | Tipo: 
    - if (isDirect)
      | Direto
    - else 
      | Indireto
  p
    | Descrição: 
    =financing.description
  p
    | Projeto:     
    - if (financing.project)
      a(href='/projects/'+financing.project._id)=financing.project.title
    - else
      | Não há projetos associados a este financiamento!
  p 
    | Beneficiário: 
    - if (financing.beneficiary)
      a(href='/organizations/'+financing.beneficiary._id)=financing.beneficiary.name
    - else
      | Não um beneficiário associado a este financiamento!  
  p
    br
    form.center.form-inline.confirm(action="/financings/"+financing._id, method="post")
      input(type='hidden', name='_csrf', value="#{csrf_token}")
      a.btn(href='/financings/'+financing._id+'/edit', title="edit") Editar
      &nbsp;&nbsp;
      input(type="hidden", name="_method", value="DELETE")
      button.btn.btn-danger(type="submit") Apagar
  p
    a(href='/financings/new') Adicionar outro financiamento 
  p
    a(href='/financings') Ir para lista de financiamentos