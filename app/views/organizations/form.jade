extends ../layouts/default


block main
  h1= title
  hr

block content
  - var action = '/organizations'
  - action += organization.isNew? '' : '/' + organization.id
  .row
    .span7
      form.form-horizontal(method="post", action=action, enctype="multipart/form-data")

        input(type="hidden", name="_csrf", value="#{csrf_token}")

        - if (!organization.isNew)
          input(type="hidden", name="_method", value="PUT")

        .control-group
          label.control-label(for='title') Título
          .controls
            input#title.input-xlarge(type='text', name="title", value=organization.title, placeholder='Até 50 caracteres.')


        .control-group
          label.control-label(for='profile') Perfil
          .controls
            textarea#desc.input-xlarge(type='text', rows="5", name="profile", placeholder='Perfil da organização.')=organization.profile
                
        .control-group
          label.control-label(for='activity') Atividades Exercidas
          .controls
            select(class='chosen-select', data-placeholder="Digite para buscar..." name="activity")
              - var isSelected = false
              - each activity in activities
                - if (activity._id.toString()==organization.activity._id.toString())
                  - isSelected = true
                option(selected=(isSelected),value=activity._id)= activity.description

        .form-actions
          button.btn.btn-primary(type='submit') Salvar
          &nbsp;
          a.btn(href='/organizations', title="cancel") Cancelar
